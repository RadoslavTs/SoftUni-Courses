list_check = ["Gold", "Silver", "Bronze", "Medallion", "Cup"]
count = 3
remove_list = [word for word in range()]
# remove_list = list(map((lambda x: x = list_check.pop() in range(count), list_check)))
print(remove_list)
print(list_check)